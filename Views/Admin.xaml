<Window x:Class="GestiondeVentaZamira.Views.Admin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin" Height="400" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="0" HorizontalAlignment="Left" >
            <TextBox x:Name="campoId" Width="100" Margin="0,0,10,0" 
                     VerticalAlignment="Center"
                     ToolTip="ID del usuario"/>
            <TextBox x:Name="campoNombre" Width="200" Margin="0,0,10,0"
                     VerticalAlignment="Center"
                     ToolTip="Nombre del usuario"/>
            <Button x:Name="btnAgregar" Content="Agregar" Width="100" Click="btnAgregar_Click"/>
        </StackPanel>

        <DataGrid x:Name="tablaUsuarios" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdUsuario}" Width="100"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>


